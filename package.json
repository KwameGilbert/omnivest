{
  "name": "omnivest-email-api",
  "version": "1.0.0",
  "description": "Simple email API for OmniVest",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "build-client": "cd ./client/ && npm run build && cd ..",
    "prestart": "npm run build-client",
    "dev": "nodemon index.js",
    "predev": "npm run build-client"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.1",
    "express": "^5.1.0",
    "nodemailer": "^7.0.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
